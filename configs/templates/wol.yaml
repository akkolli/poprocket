wol_targets:
  - id: "target_id"
    name: "Target Name"
    mac: "AA:BB:CC:DD:EE:FF"
    broadcast_ip: "192.168.0.255"
    udp_port: 9
    scopes:
      - "wol:wake:target_id"

actions:
  - id: "wake_target"
    title: "Wake Target"
    kind: "wol"
    target_id: "target_id"
    requires_confirmation: true
    scopes:
      - "wol:wake:target_id"
